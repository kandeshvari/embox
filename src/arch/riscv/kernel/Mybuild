package embox.arch.riscv.kernel

module boot extends embox.arch.boot {
	source "boot.S"
	source "trap.c"
}

module arch extends embox.arch.arch {
	//source "arch.c"
}

module libarch extends embox.arch.libarch {
	source "setjmp.S"
}

module locore extends embox.arch.locore {
	depends interrupt
}

module interrupt extends embox.arch.interrupt {
        //source "interrupt.c", "ipl_impl.h"
        source "ipl_impl.c", "ipl_impl.h"
        //depends exception
        //depends embox.kernel.irq
}

/*
module libarch extends embox.arch.libarch {
}

module context extends embox.arch.context {
	source "context.c"
}

module locore extends embox.arch.locore {
}

module clock extends embox.arch.clock {
}
*/
